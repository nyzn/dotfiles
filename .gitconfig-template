[alias]
  cp = pull
  co = checkout
  cot = checkout --track
  cob = checkout -b
  coo = !git fetch -p && git checkout
  br = branch
  st = status
  aa = add -A
  cm = commit -m
  cam = commit --amend 
  fush = push -f
  save = stash push
  pop = stash pop


